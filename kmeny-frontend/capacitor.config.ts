import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'cz.fairwebgames.kmeny',
  appName: 'kmeny',
  webDir: 'dist',
  bundledWebRuntime: false
};

export default config;
